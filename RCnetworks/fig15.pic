.\" ======================================================
.\" ## Pull in circuit library ##
.\" ======================================================
.PS
copy "gr_circ.pic"
.PE

.\" ======================================================
.
.PS
##showgrid(3,1)

move down 0.6
right
terminal()
wire(2)
junc
{ up
  wire(2)
  right
  wire(1);resistor(R1,,r);wire(1)
  junc
  {
    down
    wire(6)
    capacitor(C1,,dl)
    wire(2)
    right
    wire(2)
    junc
    down
    wire(1)
    chassisground  
  }
  wire(5);resistor(R1,,r);wire(1)
  down
  wire(2)
  junc
}
down
wire(2)
right
wire(2)
capacitor(C2,,r)
wire(6)
junc
{
  down
  wire(1)
  resistor(R2,,d)
  wire(1)
  left
  wire(2)
}
wire(2)
capacitor(C2,,r)
wire(2)
up
wire(2)
right
wire(2)
terminal()

.PE
