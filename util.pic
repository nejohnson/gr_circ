## UTLITIES ###########################################

define showgrid {
  { linethick = 0.1
    for x = 0 to $1 by 0.2 do
    {
      for y = 0 to $2 by 0.2 do
      {
        line from (x, 0) to (x, -y) dotted
        line from (0, -y) to (x, -y) dotted
      }
    }
  }
}

